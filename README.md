# 🛠️ Веб-сервис "Заявки в ремонтную службу"

Тестовое задание: веб-приложение для приёма и обработки заявок в ремонтную службу с разделением по ролям (диспетчер/мастер).

## 🌟 Live Demo

**[https://repair-requests.onrender.com](https://repair-requests.onrender.com)**

[![Deploy on Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/yourusername/repair-requests)

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose (или Colima на macOS)
- Git

### Установка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/firefirefile/repair-requests
cd repair-requests

# Запустить контейнеры (если используешь Colima)
colima start
docker-compose up -d

# Установить зависимости и настроить приложение
docker-compose exec app composer install
docker-compose exec app cp .env.example .env
docker-compose exec app php artisan key:generate

# Запустить миграции и сиды
docker-compose exec app php artisan migrate:fresh --seed

# Готово! Открой в браузере
open http://localhost:8080
```

## ☁️ Развёртывание на Render.com

### Автоматическое развёртывание (One-Click Deploy)

Нажмите кнопку **"Deploy on Render"** выше, чтобы автоматически развернуть приложение на Render.com.

### Ручное развёртывание

1. Создайте новый Web Service на Render.com
2. Подключите ваш GitHub репозиторий
3. Настройте параметры:
   - **Environment**: Docker
   - **Build Command**: `composer install --no-dev && php artisan optimize`
   - **Start Command**: `php artisan serve --host=0.0.0.0 --port=8080`
4. Добавьте базу данных PostgreSQL или MySQL в разделе **Databases**
5. Настройте переменные окружения (см. ниже)
6. Нажмите **Create Web Service**

### 🔐 Переменные окружения (Environment Variables)

При развёртывании на Render.com необходимо настроить следующие переменные:

#### Обязательные переменные:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `APP_ENV` | Окружение приложения | `production` |
| `APP_DEBUG` | Режим отладки | `false` |
| `APP_KEY` | Ключ приложения Laravel | `base64:...` |
| `APP_URL` | URL приложения | `https://your-app.onrender.com` |
| `DB_CONNECTION` | Тип базы данных | `mysql` или `pgsql` |
| `DB_HOST` | Хост базы данных | *автоматически из Render Database* |
| `DB_PORT` | Порт базы данных | `3306` (MySQL) или `5432` (PostgreSQL) |
| `DB_DATABASE` | Имя базы данных | *автоматически из Render Database* |
| `DB_USERNAME` | Пользователь БД | *автоматически из Render Database* |
| `DB_PASSWORD` | Пароль БД | *автоматически из Render Database* |

#### Рекомендуемые переменные:

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `LOG_CHANNEL` | Канал логов | `stderr` |
| `LOG_LEVEL` | Уровень логов | `info` |
| `SESSION_DRIVER` | Драйвер сессий | `database` |
| `CACHE_STORE` | Драйвер кэша | `database` |
| `QUEUE_CONNECTION` | Драйвер очередей | `database` |
| `MAIL_MAILER` | Почтовый драйвер | `log` |

#### Примечания:

- `APP_KEY` генерируется автоматически Render.com при первом деплее (если указан `generateValue: true` в render.yaml)
- Переменные для базы данных (`DB_HOST`, `DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD`) автоматически подтягиваются из прикреплённой базы данных Render, если использовать синтаксис `fromDatabase` в render.yaml
- Для работы сессий и кэша требуется запустить миграции: `php artisan migrate`

### 📁 Структура проекта

```
repair-requests/
├── app/                    # Исходный код приложения
│   ├── Http/
│   │   ├── Controllers/   # Контроллеры
│   │   └── Middleware/    # Middleware
│   ├── Models/            # Модели Eloquent
│   └── Providers/         # Service Providers
├── config/                # Конфигурационные файлы
├── database/
│   ├── migrations/        # Миграции
│   └── seeders/          # Сиды
├── public/               # Публичная директория
├── resources/
│   ├── views/            # Blade шаблоны
│   └── css/              # Стили
├── routes/               # Маршруты
├── storage/              # Хранилище
├── composer.json         # Зависимости PHP
├── Dockerfile           # Docker конфигурация
├── render.yaml          # Конфигурация Render.com
└── README.md            # Этот файл
```

### 🔧 Разработка

```bash
# Установка зависимостей
composer install
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск artisan команд
php artisan migrate
php artisan db:seed
```

### 🧪 Тестирование

```bash
# Запуск тестов
./vendor/bin/phpunit

# Или через composer
composer test
```

### 📝 Лицензия

MIT
